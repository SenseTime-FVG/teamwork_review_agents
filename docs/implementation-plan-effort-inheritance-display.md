# 推理 effort 继承值展示实施计划

1. 补充 Codex 基座模式的只读 effort 诊断，与现有运行器共享默认值常量。
2. 建立前端纯展示解析函数，接入全局默认模型和两级回退链。
3. 增加具体值、来源与模式差异的回归测试，验证继承空值和草稿不被改写。
4. 执行相关后端回归、前端测试和生产构建；检查变更后提交并推送当前分支，不重启服务。

## 验证结果

- 后端相关回归 217 项通过；全量 1375 项通过、8 项跳过，保留两条既有依赖弃用警告。
- 前端 72 项测试通过，TypeScript 和生产构建通过；保留既有包体积提示。
- 使用隔离的用户配置对照运行器，验证默认、low、high 的展示一致；未修改真实配置、默认模型或后台服务。
