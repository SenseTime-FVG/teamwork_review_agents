# 仓库环境变量优先级实施方案

1. 将统一 Agent 环境解析器合并顺序改为全局、Agent、仓库、系统运行变量，保留独立的 Provider Token 和仓库准备解析逻辑。
2. 更新界面、配置示例和文档中的优先级说明，明确整项覆盖与已有同名配置行为变化。
3. 添加后端优先级与凭据边界回归、前端说明接线测试，运行相关及全量测试并重建前端资源。
4. 保留工作区现有修改，不改真实配置、不重启服务，不提交或推送。

## 验证结果

- 环境、Web、定时任务及模型运行相关测试：61 项通过。
- Python 全量回归：1140 项通过、8 项跳过。
- 前端测试：53 项通过，TypeScript 与生产构建成功；保留原有的大文件体积提示。
- Python 编译和差异空白检查通过；未修改实际环境配置或重启后台。
