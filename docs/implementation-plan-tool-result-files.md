# 工具结果补读实施计划

1. 移除历史管理器的 4KB 首尾截短，增加含明确字节单位的正文阈值，废弃旧字段。
2. 增加运行级结果文件存储，在已授权临时目录写入脱敏 JSON，返回可校验的文件引用和分段读取提示。
3. 运行器按正文大小及当前窗口选择完整正文或文件引用，保存失败不得退化为缺失证据继续运行。
4. 修正底层 1MB 静默截断，超出资源保护上限时终止命令并准确报告。
5. 增加新日志呈现、更新配置示例和 README，保留历史截断日志的兼容显示。
6. 补充本地模拟回归，运行 Python/前端测试、编译与构建；按既有授权提交、推送并检查跨平台 CI。
