# 运行概览统一仓库筛选实施方案

1. 为状态统计及 `/api/status` 增加可选仓库范围，保持无参数的行为兼容。
2. 从已有仓库扫描水位读取最近成功扫描时间，与全局服务状态分开返回。
3. 前端以单一仓库状态构造概览统计和列表请求，复用暗色下拉组件并调整窄屏布局。
4. 保留全局服务状态刷新；概览范围响应独立存储，使用请求序号屏蔽过期响应。切换仓库重置分页、批量选择和详情。
5. 补充后端统计/API 测试，覆盖多仓库、空范围、子 Agent、定时运行、历史记录与扫描时间；补充前端查询及接线回归测试。
6. 更新 README，执行定向和全量测试、前端构建，检查差异后提交并推送当前分支。保留用户无关文件，不重启部署服务。

## 验证记录

- Python 全量：989 项通过、8 项跳过；编译检查通过。
- 前端：31 项测试通过，生产构建通过；保留既有包体积提示。
- Playwright 无界面交互：使用独立静态服务和模拟数据，确认统计及两张列表同步切换；停用仓库、空仓库、恢复全部仓库均正常。
- 从第 2 页勾选后切换仓库，分页返回第 1 页且勾选清空；人为延迟 A 仓库响应后快速切到 B，旧响应不会覆盖新范围。
- 1440、900、800、390 像素视口的顶部选择器均位于标题栏内，保持暗色背景。没有打开设计预览或操作现有服务。

## 交互修正实施

1. 仓库选项按展示名称检测重名，仅重名时追加 ID；顶部与局部下拉共用选项生成器。
2. 为仓库下拉启用按内容拓宽及水平视口定位，不换行；单独保留选择框宽度和完整名称悬停说明。
3. 列表增加独立仓库状态，查询采用“顶部具体仓库优先，否则局部仓库”的规则；统计查询始终只用顶部范围。顶部切换清空局部仓库、分页、勾选与详情，不改变表头排序。
4. 扫描和暂停／恢复按钮移除文字中的“（全局）”，增加 2 秒延迟暗色说明及键盘支持，不改变按钮实际行为。
5. 补充选项、范围组合、边界定位及接线测试；通过无界面交互覆盖长名称、同名、独立筛选、快速切换及提示计时。重新构建后提交推送，不修改部署配置或重启服务。

### 交互修正验证记录

- 前端 39 项测试通过，生产构建通过；Python 全量 990 项通过、8 项跳过。
- Playwright 无界面模拟验证：仅同名仓库追加 ID，长名称菜单保持单行且不越出视口；两个局部筛选互不影响统计或另一张列表，切换顶部仓库后局部范围与分页正确重置。
- 悬停不足 2 秒不显示说明；移开、点击及 Escape 会收起说明，键盘聚焦可查看，提示采用暗色样式。
- 在 1440、900、800、390 像素视口中检查菜单及带自定义分页数量的筛选栏，控件均保持在视口与卡片边界内。验证仅使用独立静态服务和模拟接口，未操作部署中的服务。
