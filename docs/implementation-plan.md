# 实施方案

## 阶段一：项目骨架与配置

- 建立 Python 包、命令行入口和示例配置。
- 使用 Pydantic 校验 Provider、仓库、Agent 与规则引用关系。
- 所有相对路径以配置文件所在目录为基准解析。

验收：配置错误能在启动阶段给出明确提示；示例配置可以通过 `validate`。

## 阶段二：扫描与事件

- 实现 GitHub/GitLab API Adapter。
- 规范化变更请求快照。
- 实现快照差异检测和稳定事件 ID。
- 通过 SQLite 事务同时保存快照和事件。

验收：相同快照重复扫描不会重复产生事件；字段变化会生成对应事件。

## 阶段三：规则与运行器

- 实现规则条件操作符。
- 实现事件和 Agent 运行幂等。
- 实现 SQLite 资源租约与续期。
- 使用 `codex exec --json --ephemeral` 执行 Agent 并解析最终消息。

验收：读 Agent 可并行；同一资源上的写 Agent 串行；超时和非零退出码能正确记录。

## 阶段四：Sub-agent MCP

- 提供 STDIO MCP Server。
- 实现 `invoke_agent` 工具。
- 校验调用白名单、递归深度和根任务总调用次数。
- 将父子运行关系和同一根任务 ID 写入审计记录。

验收：允许的 Agent 可以被调用；越权、超深和超量调用会被拒绝。

## 阶段五：验证与交付

- 为事件检测、规则匹配、锁和 sub-agent 策略编写测试。
- 提供 `scan-once --dry-run`，便于在不启动 Agent 时验证 Provider 与规则。
- 完善 README、示例提示词与故障日志。

验收：测试通过；CLI 帮助和配置校验可运行；无真实 Token 时也能完成离线单元测试。

## 阶段六：分层环境与 Prompt 模板

- 增加全局、仓库和 Agent 环境变量。
- 支持字面值、宿主机环境引用、Secret 与进程/Prompt 暴露策略。
- 实现 `${{ENV_NAME}}` 渲染、缺失变量空字符串和运行变量注入。
- 在日志、Prompt 快照和 Agent 结果落库前统一脱敏。

验收：覆盖优先级正确；缺失变量为空；Secret 默认不进入 Prompt 或日志。

## 阶段七：后台 API 与热加载

- 增加 FastAPI 服务与服务生命周期管理。
- 增加配置读取、校验、原子保存、版本记录和文件热加载。
- 增加立即扫描、暂停、恢复、健康检查和状态接口。
- 增加可选管理员 Token 认证。

验收：UI 保存无效配置不会破坏当前运行；有效配置在下一任务生效；后台可长期运行。

## 阶段八：实时日志与管理 UI

- 将 Codex JSONL 与 stderr 改为流式读取和逐条持久化。
- 增加日志游标查询与 SSE。
- 实现 React 仪表盘、仓库、环境变量、Agent、规则和运行页面。
- 构建静态资源并由 FastAPI 同源提供。

验收：运行中可实时看到日志；刷新后可继续查看历史；UI 能完成全部配置编辑。

## 阶段九：本地服务进程管理

- 增加 `run` 前台运行命令，并保留 `serve` 兼容入口。
- 增加 `start` 后台启动、`stop` / `end` 停止和 `restart` 重启命令。
- 使用配置专属 PID 文件和进程锁阻止重复启动。
- 后台进程继承启动环境，将控制台输出写入 `data/` 日志。
- 停止时校验 PID 与进程启动时间，优雅退出超时后再强制结束。

验收：后台启动后终端可以退出；重复启动不会产生第二个实例；停止和重启能正确管理对应配置的服务进程。

## 阶段十：已扫描 MR / PR 可见性与事件补发

- 在状态存储中增加快照列表、快照状态统计和独立事件入队能力。
- 增加已扫描 MR / PR 查询接口，以及对单个快照补发 `change_request.discovered` 的幂等接口。
- 在概览中分别展示快照数量与变化事件数量，并增加已扫描 MR / PR 列表。
- 补发前展示影响确认；补发后唤醒后台按当前规则调度，已补发项目禁用重复操作。

验收：首次只建立基线的 MR / PR 能在概览中看到；补发操作只产生一条稳定事件；重复补发不重复入队；没有匹配规则时不启动 Agent。

## 阶段十一：规则多选交互

- 将触发事件、触发 Agent 和限制仓库从原生多选框改为复选框列表。
- 支持直接点击切换多个选项，并显示当前已选数量。
- 保持配置结构与后端规则匹配逻辑不变。

验收：无需按住 `Command/Ctrl` 即可连续选择多个事件、Agent 或仓库；取消选项与保存后回显正确。

## 阶段十二：重复变化、规则级单轮去重与 MR 输入契约

- 将远端更新时间纳入事件 ID，允许相同状态转换在不同时间重复产生事件，同时保持重复扫描幂等。
- 为事件记录增加扫描批次标识，并按同一批次、仓库和 MR / PR 编排规则任务。
- 为规则增加 `deduplicate_per_scan`；启用后把同一批次中匹配的动作合并，并让每个目标 Agent 只运行一次。
- 将 Codex Prompt 的动态上下文收敛为 `mr` 对象和 `mr.action` 数组，不传递新旧快照、变化字段或内部匹配字段。
- sub-agent 只继承仓库上下文，不自动接收 `mr` 与动作数组；父 Agent 通过 `invoke_agent` 指定目标、`task` 和可选补充上下文，并自行决定是否传递 MR 信息。
- 在 UI、示例配置和 README 中解释去重边界及轮询无法恢复未观察中间状态的限制。

验收：同一 PR 的两次独立 reopen 被扫描器观察后产生不同事件；同一快照重复扫描不重复入队；规则去重关闭时多个事件分别运行，开启时同一规则和 Agent 只运行一次且 `mr.action` 包含全部匹配动作；sub-agent 的任务由父 Agent 指定，默认 Prompt 不包含根 Agent 的 MR 数据。

## 阶段十三：规则级 sub-agent 工作区继承

- 为规则增加 `inherit_workspace`，默认关闭，并在根 Agent 的 MCP 调用上下文中携带该策略和当前绝对工作目录。
- 开启时让整条 sub-agent 调用链复用当前工作目录，使父 Agent 的当前分支、暂存区和未提交文件对子 Agent 可见。
- 关闭时为 sub-agent 创建基于 MR / PR Head 的独立 detached Git worktree，并保留有修改的工作目录供审计和后续处理。
- 对共享工作区的 sub-agent 委托串行化，同时沿用根运行 ID 的可重入工作区锁。
- 在 UI、示例配置和 README 中说明该选项只继承文件系统与 Git 状态，不自动继承 MR / PR 输入或父 Agent 对话。

验收：默认 sub-agent 的工作目录与父 Agent 不同且指向 MR / PR Head；开启规则选项后父子 Agent 的工作目录完全相同，父 Agent 的当前分支和未提交文件可见；嵌套 sub-agent 延续同一策略；同一父 Agent 的共享工作区委托不会并发写入。

## 阶段十四：Agent 工作区说明与长名称编辑

- 在 Agent 页面说明根 Agent 的工作目录由触发仓库决定，而不是 Agent 自身绑定。
- 说明同一仓库只读运行可并发、声明本地仓库写操作的运行按工作目录串行。
- 扩展 Agent 名称编辑区，使较长名称无需横向滚动或频繁移动光标即可查看和修改。

验收：Agent 页面无需查阅外部文档即可理解工作区来源与并发边界；常见长 Agent 名称能在桌面宽度下完整显示。

## 阶段十五：每次 Agent 运行独立 worktree

- 将仓库配置目录收敛为基础 Git 仓库，每次根 Agent 和未继承父目录的 sub-agent 按运行 ID 创建独立 detached worktree。
- 将本地写锁从物理工作目录调整为仓库与 MR 源分支，保证同一分支写入串行、不同分支并行。
- 开启规则工作区继承时，让 sub-agent 复用父 Agent 本次运行的临时 worktree；继承者不负责删除父工作区。
- 成功且无本地遗留或提交已推送时立即删除；失败、脏文件和未推送提交保留，并按 `worktree_retention_days` 延迟清理。
- 在仓库根目录忽略默认数据目录下的 `/data/worktrees/`，防止运行 worktree 和保留标记进入版本控制，但不以 Git 忽略代替生命周期清理。
- 在 Agent 页面、仓库页面和运行详情展示基础仓库、临时工作区、并发边界及清理状态。
- 为 worktree 创建、隔离、继承、分支锁、立即清理和保留状态补充测试。

验收：同仓库不同 MR/分支的根 Agent 工作目录不同并可并发；同一源分支的写 Agent 仍串行；继承 sub-agent 与父 Agent 路径一致；无修改运行结束后 worktree 被删除；脏或未推送工作区被保留并可在 UI 中识别。

## 阶段十六：SKILL 管理与 Agent 独立装载

- 增加顶层 `skills` 配置、Skill 目录与 `SKILL.md` 元数据校验，并校验 Agent 的技能引用。
- 增加受管 `./skills/` 目录、文件夹导入接口、路径安全限制和已有 Skill 列表接口。
- 在每次 Codex CLI 运行的临时 worktree 中投影已配置技能，并按当前 Agent 显式启用或禁用应用管理的技能；运行结束后清理投影。
- 增加 SKILL 管理页面，并在 Agent 页面为每个 Agent 提供独立多选。
- 明确 sub-agent 使用自身技能配置，`inherit_workspace` 不改变技能选择。
- 更新示例配置、README、后端测试和前端构建产物。

验收：合法 Skill 文件夹可从 UI 导入并显示元数据；非法或越界目录被拒绝；Agent 只能引用已配置 Skill；不同 Agent 在同一工作区运行时各自只启用所选的应用 Skill；sub-agent 不继承父 Agent 的技能列表；运行完成后业务仓库不残留 Skill 投影文件。

## 阶段十七：GitHub Timeline 增量事件与快照对账

- 增加统一的 Provider 活动模型与活动批次，保留平台稳定 ID、动作类型、发生时间和最小必要数据。
- 为 Provider 增加可选的单 MR / PR 活动读取接口；GitHub 实现 Issue Timeline 分页与重叠游标，GitLab 暂时回退快照差异。
- 在 SQLite 中按 Provider、仓库和编号持久化不透明活动游标，并与快照和事件原子提交。
- 每轮候选筛选前为已有快照补齐 Timeline 基线；首次启用时不重放历史，游标丢失时安全重置基线，并继续用快照差异对账。
- 将 `closed`、`reopened`、`merged`、`committed`、`head_ref_force_pushed`、Draft 和标签活动转换为现有语义事件，并为每个动作配套生成 `updated`。
- 活动事件保留发生时的旧、新快照，同时携带本轮最终快照；规则匹配使用事件快照，Agent 输入、环境、工作区和锁使用最终快照。
- 活动应用后的中间快照继续与最终详情比较，只补充 Timeline 未覆盖的审批、流水线、可合并状态和元数据变化，避免重复事件。
- 增加 Provider 分页游标、首次基线、同周期 `open -> closed -> open`、新增 commit、游标重试和状态存储原子性的测试。
- 更新 README，说明 PR API、Timeline、快照和未来 Webhook 的职责边界。

验收：关闭并在下一轮扫描前重新打开仍产生按顺序排列的 `closed`、`updated`、`reopened`、`updated`；新增 commit 产生稳定且可重试的 `commits_changed`；重复读取相同 Timeline 游标不重复入队；审批和流水线等非 Timeline 字段仍由快照差异产生；首次升级不会自动重放历史事件。

## 阶段十八：扩展通用代码审核标准

- 增加改动范围和爆炸半径检查，要求评估变更与 MR / PR 目标的关系、共享组件的其他消费者以及回归与回滚边界。
- 增加仓库卫生检查，识别意外提交的运行、调试、缓存、临时、生成产物和大型二进制，同时允许项目明确需要版本化的 fixture 与产物。
- 增加路径可移植性检查，覆盖开发机绝对路径、当前工作目录假设、易变层级和临时路径的耐久性误用，但不绝对禁止合理的相对路径或系统路径。
- 增加配置命名与归属检查，要求与仓库现有命名空间、组件作用域、第三方命名和兼容策略保持一致。
- 明确新检查不自带阻塞等级，问题仍统一按通用 Prompt 的证据充分性、变更归因、现实可达性、实质影响和可恢复性判定。
- 不引入 AI、Agent、Workflow 或 aGen 的专属架构规则。

验收：通用 Prompt 能明确引导审核者检查上述四类问题；不会仅因改动较广、未拆分、路径或命名风格就阻塞合并；不含 aGen 的目录、变量前缀、Workflow 入口、Agent 工具或 Router 契约等项目专属要求。

## 阶段十九：Codex CLI 运行时默认配置

- 增加 `runtime.codex` 配置模型，覆盖默认模型、推理强度、快速模式、输出详细度、交互风格、联网搜索和受限高级配置。
- 为 Agent 增加对应的可选覆盖项；未填写时继承运行时默认，sub-agent 使用目标 Agent 自己的覆盖项。
- 将运行时与 Agent 参数转换为逐次 `codex exec --config` 参数，并保证 Agent 覆盖运行时、应用托管 MCP 与 Skill 参数不受高级配置影响。
- 移除强制忽略用户 Codex 配置的启动参数，使未覆盖项继续遵循 Codex 原生配置层级。
- 增加本机模型目录与模型来源检查接口；读取失败时提供安全回退，不把不确定的 CLI 内置模型显示成确定值。
- 增加“运行时配置”页面，并在 Agent 页面显示具体继承来源、模型候选和对应覆盖控件。
- 更新完整配置示例、README、后端测试和前端构建产物。

验收：运行时默认参数进入每次 Codex 命令；Agent 显式值覆盖全局运行时；sub-agent 不继承父 Agent 覆盖；高级配置不能覆盖应用安全边界；模型目录不可用时仍可编辑和运行；UI 能区分 Teamwork 默认、用户配置和 CLI/账号决定三种模型来源。

## 阶段二十：固化内置 Agent、规则模板与 Prompt

- 将当前通用审核、增量文档入口和增量文档更新三个 Agent 写入 `config_example.yaml`，保留 sub-agent 白名单、环境变量与写作用域。
- 将通用审核与增量文档更新两条规则写为可直接编辑的内置模板，并统一设置 `enabled: false`。
- 通过 `.gitignore` 精确放行三个内置 Prompt，其余 `prompts/` 内容继续忽略。
- 更新 README 与示例说明，明确内置 Agent 不等于自动运行，启用规则才会触发。
- 增加示例配置测试，校验内置 Agent、规则禁用状态、Prompt 文件存在和 sub-agent 引用关系。

验收：复制示例配置后可以在 UI 中看到三个内置 Agent 和两条禁用规则；未主动启用规则时不会触发 Agent；示例配置不包含 Provider、仓库或真实 Token；三个内置 Prompt 可被 Git 跟踪，其他本地 Prompt 仍被忽略。

## 阶段二十一：分离事件处理与 Agent 运行状态

- 增加事件到 Agent 调度的持久化关联表，使用 Agent 幂等键关联后续运行记录，并支持一次去重运行对应多个事件。
- 规则规划完成后立即把无匹配事件标记为 `unmatched`，有匹配事件标记为 `triggered`，不再让无关事件等待同批次 Agent。
- Agent 运行记录先进入 `queued`，实际开始 Codex CLI 前切换为 `running`，并兼容异常退出恢复与幂等判断。
- 在最近事件接口中聚合触发总数及排队、执行、成功、失败、超时数量。
- 在运行概览中分开展示事件状态和 Agent 进度，明确“未触发”“Agent 排队中”“Agent 执行中”等语义。
- 补充状态存储、编排、API 与 UI 测试，并重新构建静态资源。

验收：未被规则选择的 `updated` 事件在一次调度后显示“未触发”，不会跟随同批次其他 Agent 保持处理中；匹配事件可展示关联 Agent 总数与实时进度；等待资源锁时为排队中，Codex CLI 启动后才为执行中；规则去重时所有被合并事件都能关联到同一次运行。

## 阶段二十二：设计、历史变更与目标分支一致性审核

- 在首次工具操作中同时读取 `REVIEW_SKILLS`、`REVIEW_DESIGN_DOC_DIR` 和 `REVIEW_CHANGE_HISTORY_DIR`，将后两者定义为仓库内单目录配置。
- 非空目录配置无效时发布配置错误评论并阻止合并，不回退自动扫描；空值时扫描仓库中可能的设计和历史 change 目录，未找到时继续审核。
- 从本次 diff 设计文档、MR / PR 描述、代码、测试和配置中建立当前功能意图，扫描文档目录后完整读取所有命中的相关文档及必要引用。
- 检查当前功能与有效设计、领域不变量、对外契约、已有功能和历史变更决策是否存在无法同时成立的未解决冲突。
- 同时锁定 `REVIEW_HEAD_SHA` 与 `REVIEW_TARGET_SHA`，以 merge base `O`、目标基准 `T`、源分支 `H` 和拟合并结果 `M` 建立四版本一致性审核。
- 审核 `O → T`、`O → H` 与 `T → M`，对文件、代码块、符号、控制流、契约、schema、配置、路由、注册项和测试的重叠变更进行语义检查，不仅依赖 Git 文本冲突。
- 将意外覆盖、回退、破坏目标分支功能或与设计和历史决策存在的已确认未解决冲突列为阻塞问题；有完整替代、兼容、迁移、回滚和测试证据的正常演进不误判为冲突。
- 任一基准 SHA 变化时立即结束，不评论、不切换到新基准继续审核、不合并。
- 在最终评论中记录双 SHA 基准、设计文档和历史变更的目录来源以及一致性结论。
- 不扫描或比较其他尚未合入目标分支的打开 MR / PR。

验收：环境变量非空时只使用指定单目录，为空时能发现相关目录；无文档目录的仓库可继续审核；无效显式目录阻止合并；审核结论能用本次变更、既有文档或目标分支代码证据证明一致或冲突；目标分支 SHA 在审核期间变化时无评论结束；不因仅修改同一文件或符号就判定冲突。

## 阶段二十三：首次扫描周期事件回看

- 为统一 MR / PR 快照增加平台创建时间，并让 GitHub、GitLab Provider 从详情中填充该字段。
- 将仓库最近一次成功扫描时间与候选 API 的重叠水位分开；首次运行使用一个配置扫描周期作为事件回看窗口。
- 扩展 Provider 活动接口，使 GitHub 首次建立 Timeline 游标时可以从末页向前读取窗口内活动，同时保持游标幂等。
- 首次见到窗口内创建的 MR / PR 时生成 `opened` 与配套 `updated`；历史 MR / PR 不补发 `opened`。
- 对首次窗口内的关闭、重新打开、合并、提交、Draft 与标签活动反向还原初态，再按平台顺序生成完整语义事件。
- 将 `emit_initial_events` 收敛为只控制 `discovered`，并更新示例配置与 README 的首次扫描说明。
- 增加首次新建、历史 PR 近期活动、Timeline 跨页回看、时间边界和重复扫描幂等测试。

验收：服务首次运行时，五分钟扫描周期只输出最近五分钟内创建或变化的 PR 事件；更早历史 PR 不产生 `opened`；历史 PR 在窗口内关闭并重开仍分别产生 `closed` 与 `reopened`；下一轮使用已保存游标不会重复输出同一活动。

## 阶段二十四：后台 Codex 隔离、超时、版本诊断与取消

- 增加用户 MCP 默认隔离配置，解析有效 Codex Home 的 `config.toml` 并逐项关闭未在白名单中的 MCP Server，始终保留应用托管的 sub-agent 网关。
- 增加可选独立 `CODEX_HOME`、期望 Codex CLI 版本和运行诊断；管理接口展示二进制绝对路径、实际版本、缓存客户端版本和版本冲突警告。
- 增加运行时默认无进展超时与 Agent 覆盖；Runner 以 stdout/JSONL 为进展信号，区分总超时与无进展超时并终止完整进程组。
- 为运行表增加取消标记和兼容迁移，支持取消单次运行及全部后代；排队任务直接结束，跨进程执行任务轮询取消标记后终止。
- 增加取消运行 API 与运行详情按钮，并补充 `cancelled` 状态、事件聚合和工作区清理展示。
- 更新完整配置示例和 README，说明默认 MCP 能力边界、独立 Codex Home 登录、CLI 固定版本和两类超时的区别。
- 增加命令构造、版本检查、无进展终止、跨进程取消、API 和前端构建测试。

验收：未显式放行时用户 `node_repl`、浏览器或 Computer Use MCP 不会出现在后台 Agent 可用能力中；卡在无 stdout 的工具调用会在无进展窗口后停止；管理员可以取消排队或执行中的根 Agent 和 sub-agent；运行最终状态为 `cancelled` 且不遗留活跃进程；UI 能看到实际 Codex CLI 与共享模型缓存版本是否一致；指定期望版本不匹配时不会启动 Agent。

## 阶段二十五：可靠的后台服务停止、启动与重启

- 为管理健康接口增加实际服务 PID，并让后台启动流程轮询健康接口确认响应属于本次新进程。
- 移除“PID 文件和锁保持一秒即启动成功”的判定，端口占用、子进程提前退出和就绪超时统一返回失败及日志摘要。
- 在 PID 或锁文件丢失时扫描系统进程表，按托管子进程标志和配置绝对路径精确恢复服务身份，不按端口盲目结束进程。
- 让 `stop` 终止当前配置的全部已确认实例并等待真实退出，超时后再强制结束托管进程组。
- 让 `restart` 只有在旧 PID 已退出且新 PID 已通过健康检查后返回成功，任一阶段失败均保留非零退出码。
- 增加端口占用假成功、旧健康服务响应、PID/锁文件丢失、异常多实例和真实重启的集成测试。

验收：端口已由旧服务占用时新进程即使写入过 PID 文件也不能显示启动成功；运行中移走 PID 和锁文件后 `stop` 仍能找到并结束原进程；`restart` 返回成功时旧 PID 必须不存在且健康接口必须返回新 PID；不同配置或其他程序占用端口时不会被误杀。

## 阶段二十六：运行详情抽屉与消息化实时日志

- 将固定左右分栏改为全宽运行列表，补充仓库、MR / PR、规则、耗时和工作区状态等快速识别字段。
- 点击列表项后显示右侧详情抽屉，支持遮罩、关闭按钮、`Escape`、窄屏全屏模式和 sub-agent 内部跳转。
- 增加日志展示模型，把 Codex JSONL、系统事件和 stderr 转换为 Agent 消息、命令、工具、文件、系统、完成与错误卡片。
- 为 Agent 文本和最终结果提供禁用原始 HTML 的安全 Markdown 渲染，保留代码块、列表、链接与表格等常用格式。
- 合并连续重复日志，完整原始 JSON 放入按需展开区域，避免牺牲诊断能力。
- 保留 SSE 实时追加；仅在用户接近底部时自动滚动，浏览历史时显示新消息提示。
- 扩展运行摘要 API 与前端类型，补充仓库 ID 和 MR / PR 编号，并更新 README、后端测试和静态构建产物。

验收：运行页默认只显示易扫描的全宽列表；点击任意运行后详情从右侧滑入且可完整关闭；Agent 回复按 Markdown 消息显示，命令与工具细节默认折叠，错误醒目且重复项合并；实时日志不会在用户查看历史时强制滚动；原始事件、Prompt、环境和工作区信息仍可查看且敏感值保持脱敏。

## 阶段二十七：分离 Agent 文件权限与命令联网权限

- 为 Agent 增加 `network_access` 和 `network_domains`，默认禁止命令联网，并对域名、沙箱组合和重复项进行配置校验。
- 将 `workspace-write` 联网状态转换为 `sandbox_workspace_write.network_access`；白名单为空时关闭网络代理，非空时启用 `features.network_proxy` 并生成域名允许映射。
- 将联网参数纳入应用托管配置，禁止运行时高级配置覆盖命令联网和网络代理安全边界。
- 在 Agent 页面把“本地文件权限”与“命令联网权限”分区展示，按 `read-only`、`workspace-write` 和 `danger-full-access` 说明能力差异，并提供可选域名白名单编辑。
- 保持 Provider Token 从 Codex 子进程环境移除，同时保留 `HOME`，让 `gh` / `glab` 仅使用各自的系统钥匙串或 CLI 登录态。
- 将三个内置 Agent 默认设置为允许命令联网且白名单为空，不为其施加域名限制。
- 更新完整配置示例、README、前后端类型、回归测试和静态构建产物。

验收：`workspace-write` 默认不能联网；开启且无白名单时 `gh` 可通过自身登录态访问公开网络；配置白名单时非允许域名由 Codex 网络代理阻止；`read-only` 不能误开启联网，`danger-full-access` 不把白名单显示为有效隔离；Provider Token 在所有组合下都不会进入 Codex 环境；三个内置 Agent 默认联网且不受域名白名单约束。

## 阶段二十八：独立 Codex Home 账户登录与额度诊断

- 增加轻量 Codex App Server JSON-RPC 客户端，始终使用当前 `codex_binary` 与已保存的独立 `codex_home`，并限制启动、初始化、请求与退出时间。
- 增加账户状态接口，通过 `account/read` 返回脱敏后的认证类型、邮箱和套餐；ChatGPT 认证再读取额度窗口和可用的用量摘要。
- 增加浏览器登录会话管理，支持启动、状态查询与取消；同一 Codex Home 同时只允许一个登录，会话完成或超时后结束 App Server 进程。
- 不读取 `auth.json`，不接收 API Key，不把 Token、授权响应或 App Server 原始输出写入数据库和运行日志。
- 仅当已保存配置包含非空 `codex_home` 时，在运行时页面展示账户卡片和登录/重新登录按钮；未保存的路径变更必须先保存配置。
- 已登录卡片展示账号邮箱、套餐、认证方式、额度使用比例、窗口和重置时间；接口不提供的字段明确显示不可用。
- 补充 App Server 协议、登录生命周期、并发登录、API 鉴权边界、前端状态和构建测试，并更新 README 与打包静态资源。

验收：未配置独立 Codex Home 时行为与现有版本一致且不提供账户操作；配置独立目录后可以从管理 UI 完成 ChatGPT 登录或重新登录；成功后无需读取凭据文件即可显示邮箱、套餐和服务实际返回的额度；取消、失败、超时和服务重启不会遗留 App Server 子进程；任何管理 API、SQLite 或日志响应都不包含访问令牌、刷新令牌或 API Key。

## 阶段二十九：运行概览列表筛选、排序与数量控制

- 扩展快照和事件查询，支持按仓库、对应状态过滤，并支持有限条数或不加数量限制。
- 将 MR / PR 排序改为远端更新时间倒序，将事件保持为事件发生时间倒序，并为两类列表增加稳定次级排序。
- 扩展管理 API 参数校验，事件接口补充仓库过滤，快照接口补充状态过滤，全部模式不使用固定上限近似。
- 在两张概览列表右上角分别增加仓库、状态和展示数量控件，仓库选项只读取启用配置，默认全部仓库、全部状态和 10 条。
- 展示数量支持 10、20、50、全部和自定义正整数；自动刷新与手动切换沿用每张列表各自的筛选状态。
- 保持顶部统计卡为全局统计，不随表格筛选变化；补充窄屏布局和空结果说明。
- 增加状态存储与管理 API 的过滤、排序、全部和自定义数量回归测试，重新构建前端静态资源。

验收：两个列表可以独立选择任一启用仓库和有效状态；PR / MR 远端最近更新项与最近事件始终位于顶部；默认仅返回 10 条，10、20、50、自定义数量和全部模式结果准确；筛选后不会遗漏本应命中的旧记录；自动刷新不重置选择；顶部统计卡继续展示所有仓库的全局数量。

## 阶段三十：历史 PR 最新 Timeline 事件与手动触发

- 扩展 Provider 活动批次和 GitHub Timeline 游标，缓存最新一条可转换为现有规则事件的活动，包括平台活动 ID、原始类型、发生时间和最小数据。
- 首次回看窗口内的活动继续完整生成事件并自动调度；窗口内无活动的历史 PR 只保存最新活动参考，不写入事件收件箱。
- 对已有游标但缺少最新活动元数据的 GitHub 快照执行一次基线补齐，后续增量活动持续更新缓存，不由管理页面直接调用 GitHub。
- 为快照摘要增加最新事件引用；GitLab 等没有统一活动流的 Provider 明确返回不可用，不按当前状态猜测事件。
- 增加带手动来源和平台活动引用的唯一事件构造器，每次点击生成新事件 ID，只发送对应语义事件，不附带通用更新事件。
- 为后台运行时增加只处理待处理事件的唤醒请求，使扫描暂停时也能串行调度人工事件，并让现有首次事件补发复用该路径。
- 增加手动触发管理 API、确认提示、行级执行状态以及“最新事件”和“操作”列。
- 补充 Timeline 基线与增量缓存、历史窗口边界、重复手动触发、暂停调度、规则匹配、API 和前端构建测试。

验收：窗口内的全部事件保持自动记录和调度；窗口外历史 PR 不自动产生旧事件，但可以看到真实 Timeline 最新事件；点击后每次都产生一条可审计的新手动事件并触发当前匹配规则；无匹配规则时显示未触发；暂停扫描不阻塞手动调度；已有历史快照可在一次补齐后展示最新事件；页面自动刷新不重复访问 GitHub。

## 阶段三十一：概览操作应用内确认弹窗

- 增加可复用的深色确认弹窗组件，提供标题、目标信息、事件信息、规则影响、安全说明和确认操作区域。
- 将“补发首次事件”和“手动触发最新事件”的 `window.confirm` 替换为统一弹窗，不改变后端 API 和事件调度行为。
- 在弹窗中结构化展示仓库、MR / PR 编号、语义事件、可选平台原事件时间，以及是否存在候选启用规则。
- 支持关闭按钮、取消按钮、遮罩点击和 `Escape`；提交期间锁定关闭入口并显示请求状态，防止重复触发。
- 补充窄屏布局、焦点样式和遮罩层级，重新构建打包静态资源并执行回归验证。

验收：两种概览操作不再出现浏览器原生确认框；弹窗与管理界面视觉一致且信息层次清晰；管理员能在确认前准确识别目标和影响；取消路径不会调用 API；确认期间不能重复提交；后端事件类型、唯一性和调度结果与改造前一致。

## 阶段三十二：工作区准备状态与 Git 超时

- 为 Agent 运行增加 `preparing` 状态，在取得运行锁后覆盖基础仓库克隆、fetch 和隔离 worktree 准备，并在 Codex CLI 启动前切换为 `running`。
- 在运行列表、详情、全局统计和事件 Agent 聚合中单独展示“准备工作区”，取消操作覆盖 queued、preparing 与 running。
- 为运行时增加 `git_timeout_seconds`，默认 600 秒，并同步配置示例、README、前端类型和运行时配置页面。
- 重构 Git 子进程执行，使用独立进程组；超时或取消时终止 git、ssh、index-pack 等完整进程树。
- 增加不含远端地址和凭据的工作区阶段日志，记录操作名称、持续时间与结果，使首次克隆慢速问题可以在运行详情中观察。
- 补充状态迁移、取消、进程组终止、默认配置、Web API 和前端静态构建回归测试。

验收：等待锁时显示排队中，开始 Git 准备后立即显示准备工作区，Codex 启动后显示执行中；默认 Git 超时为十分钟且可配置；准备阶段可以取消并终止全部 Git 子进程；慢速克隆期间持续产生安全进度日志；超时或失败不会把不完整仓库移动到正式目录。

## 阶段三十三：基础仓库初始化与状态操作

- 增加进程内基础仓库初始化管理器，按仓库维护等待锁、初始化、更新、完成、失败和取消状态，并在服务退出时回收任务。
- 复用现有原子克隆、Git 超时、进程组终止和资源锁；有效仓库只执行校验及 `fetch --prune origin`，不创建运行 worktree。
- 增加基础仓库状态、启动初始化或更新、取消任务的管理 API，并继续受管理 Token 保护。
- 状态接口只返回仓库 ID、绝对路径、是否就绪、阶段、耗时、完成时间、磁盘占用和脱敏错误，不返回远端地址、Git 输出或凭据。
- 在仓库配置表单外增加操作区，只绑定已保存仓库；配置草稿与已保存目标不一致时禁用初始化按钮并提示先保存或取消。
- 补充初始化、更新、同仓库去重、共享锁、取消、服务退出、目录诊断、API 和前端构建测试。

验收：未初始化仓库可从 UI 启动且完成后显示已就绪；已就绪仓库可以执行增量更新；Agent 与初始化不会同时操作同一基础仓库；慢速操作可观察阶段并能取消；页面刷新不丢失当前任务；服务重启后按磁盘真值恢复状态；任何响应和错误均不暴露远端凭据。

## 阶段三十四：Agent 列表与单项配置保存

- 将 Agent 页面改为按名称排序的配置列表，展示 Prompt 来源、模型、文件权限、命令联网、Skill 和 sub-agent 摘要，并支持名称搜索。
- 点击列表行进入全宽 Agent 详情；详情默认只读，提供独立的编辑、保存、取消、重命名和删除操作。
- 新建 Agent 直接进入未保存的空白详情编辑页，取消时返回列表且不写入配置。
- 增加 Agent 级创建、更新和删除管理 API，请求携带预期配置版本，冲突时返回 `409`，不覆盖更新后的配置。
- 在配置管理器锁内基于最新 YAML 合并单个 Agent，继续执行全量配置校验、Secret 占位合并、原子替换、版本记录和后台热加载通知。
- 重命名时同步更新规则和 sub-agent 引用；删除时清理引用，保证完整配置始终通过引用校验。
- 存在未保存 Agent 草稿时，返回列表、切换 Agent 或离开页面需要使用应用内确认弹窗确认放弃。
- 补充配置管理器、Web API、重命名引用、版本冲突、前端交互和静态构建回归测试。

验收：Agent 首页不再连续展开所有配置表单；任一 Agent 可独立进入详情、编辑并保存；保存不会覆盖其他配置的并发变化；冲突会明确提示重新加载；新建、重命名和删除保持规则及 sub-agent 引用一致；取消和离开保护不会写入配置；保存后服务热加载新配置且无需重启。

## 阶段三十五：自动初始化与 Git 操作详情

- 新增 `runtime.repository_initialization_timeout_seconds`，默认 1800 秒；首次基础仓库克隆使用该超时，其他 Git 命令继续使用默认 600 秒的 `git_timeout_seconds`。
- 规则触发的 Agent 在准备工作区时自动初始化缺失的基础仓库，并复用仓库页初始化所使用的原子克隆、共享锁、取消和进程组终止机制。
- 扩展公共 Git 进度事件，记录命令用途、脱敏命令、状态、起止时间、耗时、适用超时、退出码和安全错误摘要，不记录原始输出或凭据。
- 仓库初始化管理器保留当前任务及最近一次任务的命令步骤，状态 API 提供详情数据；Agent Git 步骤继续写入运行日志并携带运行 ID。
- 仓库页状态标签和仓库行支持打开 Git 详情抽屉，活动期间自动刷新；Agent 来源提供进入对应运行记录的入口。
- 同步运行时配置 UI、示例配置、README、API 类型、静态资源和回归测试。

验收：基础仓库缺失时规则触发无需人工预初始化；首次克隆默认最多运行 30 分钟，后续每条 Git 命令默认最多运行 10 分钟；锁等待不消耗两类 Git 超时；仓库页可以查看每条脱敏命令的实时状态并取消手动初始化；Agent 来源可定位到运行记录；任何详情响应不包含 Token、认证信息或原始 Git 输出。

## 阶段三十六：gh 与 glab 本地登录文档

- 在 README 快速开始中强调 GitHub Agent 需要本机 `gh` 登录、GitLab Agent 需要本机 `glab` 登录，并要求使用启动服务的同一系统用户。
- 新增简短的 CLI 配置文档，只覆盖登录、平台主机选择、状态检查和 API 身份验证命令。
- 明确 Provider Token 只供扫描器访问平台 API，不能代替 Codex 子进程使用的 `gh` / `glab` 登录态。
- 在 README 快速开始和文档导航中加入配置文档链接。

验收：新用户可以从 README 直接识别本地 CLI 登录前置条件，并能通过链接文档完成 GitHub 或 GitLab CLI 登录及验证；文档范围不扩展到其他部署和排障内容。

## 阶段三十七：触发规则列表与单项配置保存

- 将触发规则首页改为按名称排序的列表，展示启用状态、事件、Agent、仓库范围、条件、去重和工作区继承摘要，并支持名称搜索。
- 点击列表行进入全宽规则详情；详情默认只读，提供独立的编辑、保存、取消和删除操作。
- 新建规则直接进入未保存的详情编辑页，取消时返回列表且不写入配置。
- 增加规则级创建、更新、重命名和删除管理 API，请求携带预期配置版本，冲突时返回 `409`。
- 服务端在配置锁内按原规则名称定位目标，更新时保持原数组位置，新建时追加，删除时只移除目标规则，并继续执行全量配置校验和原子写入。
- 存在未保存规则草稿时，返回列表、切换规则或离开页面需要使用应用内确认弹窗确认放弃。
- 补充配置管理器、Web API、规则顺序、名称冲突、版本冲突、前端交互和静态构建回归测试。

验收：规则首页不再连续展开所有长表单；任一规则可独立进入详情、编辑并保存；保存保持规则数组位置且不会覆盖并发配置变化；名称重复和无效引用被完整校验；新建、删除和取消行为明确；保存后后台热加载且无需重启。

## 阶段三十八：仓库列表与单项配置保存

- 将仓库配置区改为按 ID 排序的列表，展示启用状态、平台连接、远端项目、基础仓库目录和初始化状态，并支持搜索。
- 点击列表行进入全宽仓库详情；详情默认只读，提供独立编辑、保存、取消和删除操作，平台连接配置继续由页面上方全局编辑区管理。
- 已保存仓库的 ID 在详情中设为只读，新建仓库时允许填写 ID；新建取消时不写入配置，也不能启动基础仓库初始化。
- 增加仓库级创建、更新和删除管理 API，请求携带预期配置版本，冲突时返回 `409`；更新保持原数组位置。
- 删除仓库前检查触发规则引用；存在引用时拒绝并列出规则，且任何删除都不移除 SQLite 历史、基础仓库目录或 worktree。
- 将基础仓库状态、初始化、更新、取消和 Git 命令详情集中到当前仓库详情；未保存的目标变化继续禁用 Git 操作。
- 存在未保存仓库草稿时，返回列表或离开页面需要使用应用内确认弹窗确认放弃。
- 补充配置管理器、Web API、规则引用保护、版本冲突、仓库顺序、前端交互和静态构建回归测试。

验收：仓库首页不再连续展开全部长表单；任一仓库可独立查看、编辑和保存，已有 ID 不可误改；基础仓库状态与对应配置在同一详情中；引用中的仓库无法被误删；保存不会覆盖并发配置变化；删除不触碰历史和磁盘数据；保存后后台热加载且无需重启。

## 阶段三十九：Codex 参数继承值展示

- 扩展 Codex 运行能力诊断，优先使用 App Server `config/read` 获取完成分层后的有效配置，只返回模型、推理强度、服务速度、输出详细度、交互风格和联网搜索等安全白名单字段；接口不可用时安全回退到用户配置。
- 为每个可继承参数返回解析值、来源和可确定状态；使用模型目录补充默认推理强度，并使用 Codex 已公开的默认值补充可可靠确定的字段。
- 运行时配置选择框把“继承”改为“继承来源（具体值）”，无法确定时统一显示 `UNK`。
- Agent 配置基于当前 Agent、当前运行时草稿和后台 Codex 诊断计算同一套继承标签，显式展示最终继承到的具体值或 `UNK`。
- 明确提示仓库 `.codex/config.toml` 可能继续参与原生合并，页面展示的是没有单一仓库上下文时的后台全局继承值。
- 将 Codex 返回的 `service_tier=priority` 与 `service_tier=fast` 统一展示为快速模式，并在前端保留别名兜底；`default` 展示为标准模式。
- 补充配置解析、来源优先级、未知值、前端标签和静态资源构建回归测试。

验收：运行时与 Agent 页面不再出现没有值的“继承 Codex 配置”或“继承运行时默认”；已知值同时显示中文具体值和来源；未公开或无法解析的默认值严格显示 `UNK`；页面不读取或返回任何认证信息及非白名单用户配置；实际 Codex CLI 覆盖行为保持不变。

## 阶段四十：Codex 流收尾可靠性与 SQLite 连接回收

- 为 Codex 子进程 stdout/stderr 配置显式流缓冲上限，覆盖带有大体积命令输出的单条 JSONL 事件。
- 捕获并记录 stdout/stderr 读取异常的脱敏摘要；禁止静默吞掉读取任务异常，超长或损坏的 JSONL 必须使运行明确失败。
- 为子进程退出后的流读取任务增加有界排空与取消机制；取消、无进展超时和总超时必须在流管道异常时仍能返回终态。
- 保持 POSIX 进程组 SIGTERM/SIGKILL 逻辑，并确保收尾诊断不会覆盖管理员取消或既定超时原因。
- 将 StateStore 连接创建改为显式关闭的上下文管理器，统一覆盖事务成功、异常回滚和连接回收。
- 增加超长 JSONL、读取异常、终止后管道不结束、超时终态和 SQLite 文件描述符稳定性的回归测试。

验收：超过 asyncio 默认单行限制的合法 Codex JSONL 可以正常解析；无法安全读取的流使运行明确失败；子进程退出或被终止后，Runner 在固定宽限期内结束并写入正确终态；持续状态查询和取消轮询不会线性增加 SQLite 文件句柄；全量测试、前端构建和 Python 编译检查通过。

## 阶段四十一：批量手动触发最新事件

- 在运行概览 MR / PR 列表增加批量选择模式、行首复选框、取消和带已选数量的手动触发按钮；没有最新事件的行不可选择。
- 仓库、状态或展示数量筛选变化时清空选择，避免触发当前不可见记录。
- 扩展应用内确认弹窗，汇总目标数量、仓库分布、事件类型分布和候选规则影响。
- 新增批量管理 API，逐项复用单条最新事件创建语义，拒绝重复目标并返回每项成功或失败结果；单项失败不影响其他项。
- 批量写入完成后仅唤醒一次调度器，并刷新概览、事件和运行统计。
- 增加批量接口、重复目标、部分失败、前端类型和生产构建回归检查。

验收：管理员可以选择多个当前列表中的 MR / PR，并让每一项按自己的缓存最新事件进入规则引擎；无最新事件记录不可选择；单项错误不会阻塞其他事件创建；确认弹窗和完成提示可准确说明批次影响；筛选变化不会遗留隐藏选择。

## 阶段四十二：运行取消应用内确认弹窗

- 移除运行详情取消操作中的浏览器原生 `window.confirm`。
- 复用现有危险操作确认弹窗，展示 Agent、仓库与 MR / PR、运行 ID 和当前状态。
- 明确提示全部 sub-agent 会收到取消请求，并且当前 Codex 或 Git 进程组可能被终止。
- 请求期间锁定弹窗关闭入口并显示处理状态，避免重复提交；取消弹窗不关闭运行详情。
- 保持既有取消 API、运行刷新、错误提示和后端递归取消行为不变，并重新构建前端静态资源。

验收：取消运行不再显示浏览器原生确认框；弹窗视觉与管理界面一致且目标信息完整；取消和关闭不发送请求；确认只能提交一次，完成后刷新运行状态；底层运行详情在整个确认过程中保持打开。

## 阶段四十三：触发规则列表内联启停

- 将触发规则列表状态列从只读文字改为启用开关，同时保留详情页中的启用配置。
- 点击开关时阻止列表行导航；点击行内其他区域或末尾详情按钮仍进入对应规则详情。
- 复用规则级更新 API 提交完整规则和预期配置版本，保持原子写入、完整校验、版本冲突保护与后台热加载。
- 请求期间锁定列表启停操作并显示保存状态；成功后更新配置与版本，失败时保持原状态并展示错误。
- 调整桌面与窄屏列表布局，避免状态开关挤压事件、Agent 和仓库摘要。
- 补充规则启停 API 回归断言，并重新执行前端生产构建、Python 测试和编译检查。

验收：规则可以直接从列表启用或停用，点击开关不会打开详情；详情中的启用开关仍可用且与列表状态一致；重复点击不会并发提交；配置版本冲突不会覆盖新配置；保存成功后服务热加载且无需重启。

## 阶段四十四：审核目标分支权威 SHA

- 在 Agent 工作区完成远端 fetch 后，从 `refs/remotes/origin/<目标分支>` 解析目标分支当前提交，并在 Codex 启动前固定为本轮 `target_head_sha`。
- 将 `target_head_sha` 与现有 `target_ref` 一起加入根 Agent 的 `mr` 上下文；目标 ref 缺失或无法解析时让工作区准备明确失败，不使用 PR/MR 的 base SHA 回退。
- 修改通用审核 Prompt，明确 `REVIEW_TARGET_SHA` 只能取自传入的 `mr.target_head_sha`，后续只能通过真实分支 ref 的实时查询判断是否变化。
- 明确禁止使用 GitHub `base.sha`、`baseRefOid` 或语义等价的变更请求基准字段判断目标分支是否更新；这些字段只可作为历史和差异诊断信息。
- 补充目标 ref 解析、根 Agent 上下文、工作区准备后取值以及 Prompt 禁止错误字段的回归测试。

验收：目标分支在 Agent 启动前未变化时，PR/MR 的旧 base SHA 不会导致审核误终止；传入的 `target_head_sha` 与工作区 fetch 后的远端跟踪 ref 完全一致；目标 ref 无效时 Codex 不启动；审核期间真实目标 ref 变化仍会阻止使用旧审核和旧 CI 结果执行评论或合并。

## 阶段四十五：原生 Windows 运行支持

- 增加跨平台进程控制模块，为同步和异步子进程统一生成 POSIX 新会话或 Windows 新进程组参数，并提供温和、强制的进程树终止能力。
- 使用跨平台文件锁替换直接 `fcntl.flock`，保持单配置单实例、锁探测和进程退出自动释放语义。
- 使用跨平台进程信息读取替换 `ps`、`shlex` 进程表解析和无条件 `waitpid`，继续以 PID、启动时间、模块命令和配置绝对路径确认托管服务身份。
- 改造后台服务、Codex Runner、Codex App Server、Git 工作区和 Preflight，使启动、取消、超时与停止在 Windows 上不调用 `os.killpg`、`SIGKILL` 或 `start_new_session`。
- README 增加 PowerShell 快速开始和 Windows 运行说明，运维文档区分 POSIX 信号与 Windows 进程树终止，并说明 Bash 脚本和 systemd/launchd 的平台边界。
- CLI 入口统一配置 UTF-8 标准流，避免 Windows 重定向输出使用本地代码页时因中文消息启动失败。
- 捕获 Git 与 Codex 诊断输出时显式使用 UTF-8 并安全替换非法字节；Preflight 为 Python 子命令固定 UTF-8 环境。
- Windows 终止逻辑按系统父 PID 关系补充枚举已经失去直接父进程的后代，避免继承管道或后台子命令残留。
- 新增 Windows GitHub Actions，真实执行安装、导入、配置校验、后台启停重启、进程发现和进程树回收测试；保留 POSIX 全量回归。

验收：原生 Windows 安装后导入 CLI 不依赖 `fcntl` 或 `ps`；PowerShell 中可以复制示例配置并完成 `validate`、`start`、`restart` 和 `stop`；终止服务、Codex、Git 或 Preflight 时不遗留可识别后代进程；Linux/macOS 原有文件锁、进程组与信号行为不回归；文档不再把 POSIX 命令误称为跨平台用法。

## 阶段四十六：规则级可选本地 CI

- 为触发规则增加“执行仓库 CI（如已启用）”配置，默认关闭，并在规则详情与列表摘要中展示。
- 保留仓库级 CI 主开关，在仓库详情增加状态名称、总超时、日志上限和有序命令步骤编辑器；步骤支持名称、执行程序、逐项参数和可选单步超时。
- 调整编排器，把同批次调用拆分为直接 Agent 与 CI 门禁 Agent；直接 Agent 立即启动，不等待 Preflight。
- 仅在规则要求 CI、仓库启用 CI 且 PR 当前打开时执行门禁；仓库未配置 CI 或 PR 已关闭/合并时跳过门禁并直接启动 Agent。
- CI 成功时启动门禁 Agent；失败或超时只阻断门禁 Agent；基础设施错误只影响要求 CI 的事件路径，并保留现有重试语义。
- 同一 PR Head 与配置版本复用现有 Preflight 幂等结果，不为每条规则重复运行本地命令。
- 更新配置示例、README 和 CI 专题文档，明确 CI 是目标仓库维护的一组参数数组命令，复杂流程可以调用仓库脚本。
- 补充无 CI 仓库放行、关闭/合并 PR 放行、混合规则并行、失败隔离、配置解析和前端生产构建回归检查。

验收：规则选择全部仓库时，未配置 CI 的仓库仍能正常触发 Agent；同批次不要求 CI 的规则不等待 CI；CI 失败只阻断明确选择 CI 的规则；仓库详情可以完整编辑 CI 步骤，规则详情可以独立选择是否执行；保存后后台热加载且无需重启。

## 阶段四十七：目标分支提交变化临时事件

- 为统一快照增加向后兼容的 `target_head_sha`，GitHub 通过 Git Ref API、GitLab 通过 Repository Branch API 获取目标分支真实 Head。
- 每个仓库扫描批次按目标分支缓存查询结果，同时覆盖 Provider 本轮候选和数据库中未被更新时间筛选命中的打开状态 PR / MR。
- 首次读取目标 Head 只建立基线；后续变化时每个 PR / MR 每轮最多产生一条 `change_request.target_commits_changed`，且不派生通用更新事件。
- 将目标分支变化事件作为临时可靠事件写入现有事件队列，持久化时排除 Provider 原始响应，保留服务异常恢复、失败重试和稳定幂等键。
- 成功、未匹配或仓库失效后立即删除临时事件；失败记录继续进入既有重试流程。
- 在删除事件前把仓库、编号、标题和地址固化到关联 Agent 运行记录，并让运行查询优先读取固化字段，避免运行详情依赖已删除事件。
- 更新事件选项、配置示例和 README，并补充 Provider、差异检测、扫描候选补齐、临时事件恢复清理和运行上下文回归测试。

验收：目标分支一次推进只让每个打开 PR / MR 产生一个可匹配规则的目标提交变化事件；未更新的 PR / MR 即使未进入 Provider 更新时间候选仍能检测到变化；首次升级不补发历史变化；服务异常退出不会永久漏触发；成功或未匹配事件不留在事件历史和统计中；失败可重试；事件删除后关联 Agent 运行仍完整显示仓库与 PR / MR 信息。

## 阶段四十八：Agent 每次运行临时 HOME

- 为 Agent 增加向后兼容的 `home_mode: inherit | temporary`，默认继承系统 HOME；只允许可写沙箱选择临时 HOME。
- 为每次根 Agent 和 sub-agent 创建独立的系统临时目录，预建 HOME、XDG 和 Windows 用户目录骨架，并在 Codex 及后代进程收尾后清理。
- 在服务进程首次创建 Runner 时清理所属 PID 已经不存在的遗留临时 HOME，不删除仍由存活进程持有的目录。
- 临时 HOME 模式显式固定原 `CODEX_HOME`，桥接已有 `GH_CONFIG_DIR`、`GLAB_CONFIG_DIR`、`GIT_CONFIG_GLOBAL` 和 SSH Agent 连接；不复制用户目录、私钥或凭据。
- 保持 Provider Token 最终移除规则，Agent 环境变量不能覆盖临时 HOME 的托管目录或重新注入 Provider Token。
- 运行日志增加 HOME 准备和清理事件，只记录临时路径、模式与桥接项，不读取桥接文件内容。
- Agent 详情在本地文件权限附近增加 HOME 模式配置，列表摘要显示系统或临时 HOME；配置示例和 README 说明与 worktree、Codex Home、CLI 登录态的区别。
- 补充配置组合、环境桥接、独立运行目录、正常/失败清理、遗留目录回收和 UI 类型回归测试，并执行全量 Python 测试、前端生产构建与编译检查。

验收：不同仓库和不同 Agent 不需要声明仓库专用 HOME 变量即可把 `~/.cache`、`~/.config` 等写入本次运行临时目录；Codex 与已登录的 gh/glab 仍使用原认证入口；Provider Token 不进入 Agent；所有终态都清理临时 HOME，服务异常退出后的遗留目录可在后续启动安全回收；只读 Agent 不能保存误导性的临时可写 HOME 配置。

## 阶段四十九：扫描与 Agent 执行解耦

- 将后台唯一串行周期拆分为独立扫描循环和独立事件执行循环；扫描循环只调用 Provider 扫描并持久化事件，不等待 Agent。
- 扫描完成后唤醒事件执行循环；执行期间到达的新事件保留唤醒信号，并在当前批次结束后继续领取。
- 复用 SQLite 事件收件箱、事件调度关系、Agent 幂等键和异常恢复逻辑作为可靠队列，不引入只存在于内存的运行任务来源。
- 保持最大并发 Agent、资源锁、Preflight、失败重试、取消和临时事件清理语义不变。
- 让“暂停”只暂停新扫描；已持久化事件与正在运行的 Agent 继续执行和收尾。
- 分离扫描状态、调度状态、最近摘要和错误；概览明确显示“正在扫描”，不再把长时间 Agent 误报为扫描仍未结束。
- 补充运行时并发回归测试，证明 Agent 调度阻塞时手工扫描及后续周期仍能完成，并验证暂停状态下的手工事件调度不访问 Provider。
- 更新前端状态类型和概览文案，重新构建内置静态资源，并执行全量 Python 测试和编译检查。

验收：Agent 连续运行一小时不会推迟下一轮 Provider 扫描；点击“立即扫描”无需等待当前 Agent；新事件可靠进入 SQLite 并由独立执行循环处理；服务异常退出后事件可以重新入队且不重复成功运行；暂停扫描不取消 Agent；概览的扫描状态与 Agent 状态互不混淆。
