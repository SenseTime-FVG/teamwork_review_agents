# 快速配置实施方案

1. 实现无副作用的向导配置构造器、规则范围算法及脱敏预览。
2. 接入带版本校验的原子保存接口和只读连接检查。
3. 补齐仓库级 Agent Skill 覆盖及引用维护，不修改共享 Agent。
4. 实现五步 UI、动态规则选择、Token 显隐及首次使用入口。
5. 覆盖规则矩阵、重复仓库、Secret、版本冲突和 Skill 隔离测试；构建前端并更新文档。

## 验收记录

- 后端：快速配置、Web/环境、配置与 Runner、Skill、模型循环/回退、规则和定时调度相关测试共 213 项通过。
- 前端：`npm --prefix ui test` 共 15 项通过；`npm --prefix ui run build`、Python 编译与差异格式检查通过。
- Playwright：使用独立临时配置、关闭后台调度，验证五步流程、Token 显隐、动态事件/定时规则、范围预览、保存及取消。取消前后配置文件 SHA256 一致；390px 窄屏无横向溢出，浏览器没有错误日志。
- 连接探测使用模拟 HTTP/Git 单元测试，未向真实仓库发起写操作，也未在 Windows 实机验证。前端构建仍有现有单包超过 500kB 的提示，不影响构建成功。

## 2026-09-14：初始预填与退出确认调整

1. 为显示名称和 Token 增加字段级交互许可，未操作时防止预填，操作后继续支持原生浏览器选填、键盘、粘贴和明文切换。
2. 隔离仓库与凭据表单，保留草稿跨步骤行为，切换平台/仓库/凭据来源时重置许可与对应凭据。
3. 用页面内深色模态确认替换 `window.confirm`，覆盖安全默认焦点、Esc、焦点恢复、继续编辑与放弃草稿。
4. 运行前端测试和构建；使用隔离浏览器、虚构 Token 验证交互，不操作用户当前 Chrome 凭据。记录实际浏览器验证边界。
5. 更新内置前端构建产物；验证通过后只提交本次相关文件并推送当前分支。

### 本次验证结果

- 前端测试 19 项通过，向导后端回归 34 项通过；TypeScript 和 Vite 构建成功，保留现有大包体积提示。
- 隔离 Chrome profile 中保存虚构凭据：普通登录表单可复现自动预填；新向导进入仓库步骤时两个字段仍为空，操作仓库地址和单纯点击 Token 不会自动填好。
- Playwright 验证未经操作的填入被拦截、键盘首字符不丢失、主动填入事件进入预览请求、名称不被覆盖、明文切换、跨步骤保留、已有仓库回填与凭据来源切换。
- 退出确认默认焦点、Tab/Shift+Tab 焦点循环、Esc/继续配置保留草稿、明确放弃后重开为空，以及 390px 窄屏均通过检查；无浏览器原生确认框、无保存请求。
- 无头 Chrome 无法可靠操作原生密码候选浮层，普通登录对照页也有相同限制；候选保留采用标准 `current-password` 语义，主动填入使用原生 input/change 事件验收，未声称实际候选点击已自动化验证。
- 浏览器日志没有应用错误，仅有原有管理 Token 输入框未放入 form 的提示；没有操作用户当前 Chrome 凭据或生产服务。
