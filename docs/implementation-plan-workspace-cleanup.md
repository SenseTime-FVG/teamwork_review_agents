# 运行工作区定时清理实施方案

1. 新增结构化清理计划配置，复用既有保留期字段；实现主机本地时间的下一次执行计算。
2. 增加受管工作区检查、删除专用校验、数据库活动检查及清理结果回写；运行和清理使用同一工作区租约，移除原先准备工作区时的自动过期清理入口。
3. 在后台生命周期接入独立清理循环、持久化计划和最近执行摘要，并提供只读状态接口。
4. 在配置历史上方增加暗色配置及结果卡片，支持四种计划和保留天数，复用全局保存流程，不新增立即删除入口。
5. 补充临时目录回归、API 与前端测试，加入 Windows 测试列表，运行全量回归并更新前端构建产物。
6. 完整审查后交付验证结果；不操作真实工作区或重启服务，未获提交授权时不提交或推送。

## 验证结果

- Python 全量回归：1127 项通过、8 项跳过；其中清理专项 43 项通过。
- 前端：51 项测试通过，TypeScript 与生产构建通过；构建仍有现有的大文件体积提示。
- Python 编译与差异空白检查通过；Windows CI 已加入清理专项测试，本机未执行原生 Windows 验收。
- 所有清理测试均在临时目录内完成，没有删除部署中的真实工作区，也没有重启后台服务。
