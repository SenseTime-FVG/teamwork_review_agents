# 实施计划：Git 无进展超时与实时进度

1. 增加有界进度解析和跨平台管道采集，白名单输出安全结构。
2. 将托管 Git 命令总超时改成无进展超时，保留现有配置键、取消和进程树清理。
3. 扩展进度事件并贯通仓库详情和 Agent 日志；更新前端展示、配置文案及 README。
4. 验证持续进展超过时限仍成功、重复输出不续期、停滞/取消回收进程、凭据脱敏、真实 Git 克隆及前后端兼容。
5. 隔离浏览器验证进度、空状态、旧记录及窄屏；构建内置前端。验证通过后提交并推送当前分支，不重启生产服务。

## 验证结果（2026-09-15）

- 完整后端测试：925 项通过、8 项按平台条件跳过；保留现有 FastAPI/Starlette 弃用提示。覆盖活跃下载超过原总时限仍成功、对象数不变但字节增长、重复进度/速度变化/心跳不续期、停滞超时、取消、忽略 SIGTERM 的后代清理及 Git 凭据脱敏。
- 使用真实 Git `file://` 克隆验证原生进度解析与文件内容一致；长日志用例超过 2100 条后仍能读取最新完成状态。未在 Windows 实机运行，不将 POSIX 进程树测试视为 Windows 验收。
- 前端测试 22 项通过，TypeScript/Vite 构建成功；保留现有单包体积提示。
- Playwright 使用关闭调度器的独立测试服务及虚构进度，验证秒级刷新、速度/大小、超总时限仍执行中的展示、停滞提示、未知百分比、旧记录和完成状态。1440px 与 390px 截图人工检查通过，无横向溢出；修正了进度条颜色变量。浏览器无应用错误，仅有原有管理 Token 未放入表单的提示。
- 未操作真实仓库、凭据或生产任务；后端执行逻辑需要部署后重启服务，新 Git 命令才生效。
