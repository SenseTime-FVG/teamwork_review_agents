# 仓库名称与迁移实施方案

1. 增加 `display_name` 与兼容展示，开放 ID 编辑，预览目录变化。
2. 实现单仓库配置合并、规则引用更新、路径规划与安全校验。
3. 实现带恢复日志的目录移动、Git 元数据修复和数据库迁移。
4. 增加后台维护门禁，协调扫描、定时任务及手动管理操作。
5. 使用真实本地 Git 仓库验证 linked worktree、独立 clone、未提交修改和历史查询；覆盖 Secret 保留、目标冲突、事务失败、跨盘复制及重启恢复。
6. 更新使用文档、示例配置，运行相关后端测试和前端构建。
