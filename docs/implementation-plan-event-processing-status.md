# 事件处理中状态修复实施计划

1. 修改事件状态与筛选文案，移除运行中 CI 对列表标签的覆盖，不新增细分状态。
2. 编排器选择真正受 CI 约束的代表事件并传递完整匹配事件集合。
3. 复用既有关联表：新建、重试时原子关联，缓存复用时在网络操作前关联。
4. 补充处理中实时详情、无关事件隔离及状态文案测试，更新说明与前端构建产物。
5. 执行针对性及全量回归、编译和构建，按既有授权提交推送并检查跨平台 CI。
