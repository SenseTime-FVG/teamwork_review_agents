# GitLab 活动与手动触发实施计划

1. 增加适配器能力声明与 GitLab 状态、标签、版本、详情活动归一化。
2. 实现稳定游标、分页检查、首次基线、失败可见及恢复去重；保留快照检测。
3. 扩展存储查询，提供同仓库同 Provider 当前源版本的系统重放候选；接入单条及批量手动触发。
4. 更新概览、详情和确认框，分开平台活动与手动候选，显示事件来源和错误状态。
5. 添加 Provider、编排、事件、Web、前端回归，运行全量测试与前端构建。
6. 检查完整差异，按中文 Conventional Commits 提交并推送当前分支；不触碰真实配置和无关文件。

## 验证结果（2026-09-22）

- Python 全量：1322 passed、8 skipped；新增 11 项 GitLab/编排/Web 回归。
- 前端：58 项测试通过，生产构建通过；保留现有体积告警。
- 真实 GitLab 只读验证：目标仓库 MR !39 识别为提交更新、!37 识别为合并；重复使用返回游标读取，新增活动数均为 0。
- 未把验证游标写入运行数据库，未执行规则、Agent 或远端写操作，未修改配置或重启服务。
