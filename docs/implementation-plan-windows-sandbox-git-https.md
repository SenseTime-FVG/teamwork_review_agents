# Windows 沙盒 Git HTTPS 修复实施方案

1. 抽取无密钥 askpass 文件生成逻辑，增加运行级 Git 配置合并及沙盒上下文。
2. 将 helper 精确可写目录和 Python 依赖只读目录加入现有权限档案，接入执行器的运行生命周期，保持进程 Token 暴露开关。
3. 复用模型工具的有界子进程执行器进行沙盒内只读诊断，接入模型模式与完整 CLI 模式。
4. 区分 Git 基础设施错误与普通命令失败，保存明确错误码、脱敏原因和重试策略，向父 Agent 传播阻断。
5. 增加 UI 日志标题、README 与自动化回归，补充 Windows CI 覆盖。
6. 执行定向及全量 Python 测试、UI 测试和构建；检查差异后提交并推送。

## 工作区所有权校验补充

1. 为 Git 配置合并增加有序重复键支持；要求上下文接收执行器已校验的工作区，先重置信任列表再追加精确路径。
2. 在准备步骤前完成注入并记录信任目录，不修改宿主 owner、ACL 或全局 Git 配置。
3. 新增所有权错误分类和 UI 文案，沿用不可重试错误与父 Agent 阻断链路。
4. 补充真实 Git 异主模式、clone/worktree、环境合并、并发/继承、准备步骤及运行器传递测试。
5. 运行 Python、UI 回归与构建，检查差异后按用户既定要求提交推送。

## Windows askpass ACL 修正

1. 执行器在准备步骤前创建独立 Git 运行目录；上下文显式接收根路径，仅在 `git-askpass/` 创建本轮 helper，目录规范化后精确授予 write。
2. 不搬迁 Runner 的 Codex 目录，不授权整个 TEMP，不扩大网络或 Token 暴露范围，保留 OpenSSL、证书与 safe.directory 配置。
3. MCP Broker 在该上下文下使用宿主环境及宿主凭据上下文，不再复用 Agent 工具环境；覆盖有/无 Token、被修改 helper、嵌套子 Agent 的隔离回归。
4. 在 finally 清理 helper 及其运行目录，补充成功、准备失败、取消、创建失败和目录链接清理测试。
5. 更新 README，执行定向及全量回归，提交推送；明确实际 Windows 沙盒仍需部署验收。

## Windows CI junction 清理修正

1. 在公共清理器中用 lstat 区分实际目录、符号链接和 junction；每次重试重新判断入口，仅对 junction 执行 rmdir。
2. 保留缺失路径处理和有界重试，嵌套链接的权限错误回调不能修改链接目标的权限。
3. 添加跨平台模拟测试及原生 Windows 顶层、嵌套、失效 junction 测试，保留原有 helper 安全清理用例。
4. 定向与全量测试通过后提交推送，检查本次提交的 Windows push/PR CI 结果；不修改沙盒授权或凭据方案。

## Windows 两层目录环境修正

1. 增加配对启动对象、宿主目录环境构造和沙盒内固定启动桥；仅携带目录变量，不持久化凭据。
2. 统一接入模型工具（含预检与补丁）、完整 CLI、工作区准备及能力诊断；准备步骤单独使用空临时 Codex home。
3. 补环境优先级、大小写、缺失值、代理保留、标准流、退出码及取消清理测试；提供显式启用的真实 Windows 沙盒验收。
4. 更新 README 与 Windows CI 测试集合；执行本机定向/全量测试，提交推送并查看跨平台 CI。真实 Windows 沙盒验收单独报告，不以模拟或普通 Windows CI 代替。
