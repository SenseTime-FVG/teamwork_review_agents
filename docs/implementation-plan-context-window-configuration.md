# 上下文窗口配置实施计划

1. 增加 Provider、模型节点、Agent 的可空窗口字段及旧配置迁移、预算校验。
2. 在模型解析层统一计算有效窗口和来源，回退切换、快照和压缩共用结果。
3. 为 Provider、全局主模型/回退、Agent 主模型/回退增加输入及具体继承提示；留空保存为继承。
4. 覆盖继承矩阵、旧配置迁移、配置保存、回退预算、默认值展示与表单渲染测试。
5. 更新配置示例和说明，构建前端并运行回归；提交、推送当前分支，不改动部署配置或重启服务。

## 验证结果

- Python 全量：1052 项通过、8 项跳过；最终窗口及压缩专项：117 项通过。
- 前端：43 项测试通过，生产构建成功；保留原有构建包体积提示。
- Playwright 隔离页面：验证全局清空保存为 null、Agent 切换 Provider 后继承值更新、Provider 窗口更新同时影响 Agent 主模型及回退项；检查深色表单和窄屏回退项布局。
- 未修改真实部署配置、数据库或运行服务。
