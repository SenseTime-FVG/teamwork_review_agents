# 子 Agent 等待修复实施计划

1. 增加按异步上下文隔离的运行控制状态，记录真实停止原因与子任务等待状态。
2. 在 invoke_agent 的 try/finally 边界进入和退出等待，暂停父 idle 而保留总时限。
3. 看门狗生成明确停止原因；模型循环在异步边界复查停止状态，防止吞掉取消后继续执行。
4. 执行器注入持久化取消来源，完成排队/准备中断的收尾，统一写入取消终态并传递子任务错误码。
5. 补充模拟嵌套运行及持久化回归，更新 README 的运行时限说明。
6. 执行针对性测试、全量 Python 测试和编译检查；按已有授权提交并推送，检查当前提交的跨平台 CI。
