# 原对话副本压缩实施方案

1. 重构摘要请求构造：深拷贝原请求，保留消息结构与指令，末尾追加通用交接请求；保留工具定义但禁止调用，移除任务输出 Schema。
2. 用完整副本摘要及同源有界收短替代 JSON 字符分片；保留历史原子提交、固定消息、近期回合及失败诊断。
3. 默认阈值改为 90%，预留交接请求开销，保留显式配置；同步配置示例、运行日志和现行文档。
4. 补齐 Anthropic/Gemini 禁用工具映射；测试各适配器的真实请求结构，确保工具调用与结果关系保留。
5. 覆盖副本隔离、完整前缀、固定指令、多次压缩、工具禁用、90% 边界、显式 95%、超限/取消/失败原子性、Provider 回退与不重放工具。
6. 执行定向及全量回归；核对已有失败与新增回归，检查最终差异，提交并推送当前分支。不得修改部署配置、重启服务或包含已有 `.DS_Store` 文件。

## 验证记录

- 上下文、交接、窗口配置与工具结果定向测试：140 项通过。
- 完整测试集发现两项既有失败；在修改前 `524699f` 的独立临时源码快照中分别复现：`test_combined_update_prompts_support_github_and_gitlab`、`test_builtin_prompts_render_configured_environment_values_directly`，均为组合更新 Prompt 的 Agent 名称变量断言，本次不修改该 Prompt。
- 排除上述两项后重新执行全量：1302 项通过、8 项跳过、2 项排除。前端 `npm test`：56 项通过。`git diff --check` 通过。
- 各模型协议通过模拟 HTTP 请求验证消息结构、工具禁用与失败处理；未调用真实上游模型，不宣称摘要内容绝不失真。未修改部署配置、重启服务或进行真实 Windows 部署验收。
