# 推理强度兼容降级实施方案

1. 新增共享的 effort 参数拒绝判定与降级顺序函数，分别接入外部 API 和 Codex 模型客户端的 HTTP / SSE 错误路径。
2. 在模型回合的异常处理中优先降级 effort，再走既有模型回退逻辑；记录配置档位、实际档位及有界降级轨迹。
3. 统一外部 Provider 的 effort 候选，补齐 xhigh/max，过滤 minimal；保留其他历史自定义值。
4. 更新运行消息与详情展示，区分参数降级、模型回退和真实失败。
5. 增加错误分类、协议请求、全链耗尽、多轮工具、模型切换、取消与前端展示回归测试。
6. 执行定向测试、全量 Python 测试和 UI 构建，检查 diff 后按用户既有要求提交并推送。

## 验证结果

- 新增 57 项 Python 用例，覆盖明确拒绝识别、两种外部协议、完整降级链、去掉参数后继续拒绝、多轮工具、模型回退、取消和 Codex HTTP/SSE/JSON 错误。
- Python 全量回归：489 通过，5 项平台相关用例在 macOS 跳过。
- UI 测试：2 项通过（`npm test`，使用支持 TypeScript 类型剥离的 Node.js）；UI 类型检查和生产构建通过，已同步预构建资源。
- Python 编译与 `git diff --check` 通过。
- 上游兼容性通过本地模拟响应验证，未调用用户真实模型服务，也未修改保存的 Provider 配置。
