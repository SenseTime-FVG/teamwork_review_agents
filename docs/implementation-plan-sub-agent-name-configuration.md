# 子 Agent 名称配置一致性实施计划

1. 将示例配置与当前使用说明中的两个旧变量替换为入口 Prompt 已使用的新变量，补充旧部署迁移说明。
2. 同步配置校验、Prompt 直接渲染和祖先校验测试中的名称与断言。
3. 增加示例配置到实际环境解析再到 Prompt 的联动测试，以及自定义名称和缺失变量边界测试。
4. 执行相关测试、全量 Python 测试与编译检查，确认不修改实际部署配置和未跟踪的系统文件。
5. 审查完整差异，按项目提交规范提交并推送 `dev/wudonghao`，检查 PR #30 对应 CI。

## 本地验证结果

- Prompt 渲染、配置与运行器、组合入口祖先校验、语言配置共 215 项相关测试通过。
- 全量 Python 测试：1311 项通过、8 项跳过，无失败；保留 2 条第三方库弃用警告。
- `python -m compileall -q src tests` 与 `git diff --check` 通过。
- 实际配置、服务状态、被忽略的本地 Prompt 和 `.DS_Store` 均未修改或纳入提交。
