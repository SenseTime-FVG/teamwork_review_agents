# Agent 等待与超时实施方案

1. 增加全局/仓库远端 CI 等待配置和持久化等待记录。
2. 实现可信仓库范围内的只读 CI 等待服务，接入内嵌函数工具和全部 CLI MCP 通道。
3. 按父运行 ID 应用总时限；保证受控等待不触发 idle，超时为不可重试终态。
4. 更新运行时说明、内置入口 Prompt、UI 和 README；不迁移历史运行、不重启服务。
5. 增加隔离回归，运行后端/前端测试及构建，检查完整 diff 后仅提交本次文件并推送现有分支。

## 验收记录

- 后端完整回归：`python -m pytest -o addopts='' -q`，981 通过、8 跳过；保留既有 Starlette/AnyIO 弃用警告。跳过的真实平台测试不代表已在 Windows 原生环境验收。
- 远端 CI 专项：31 项通过，覆盖固定截止时间、平台状态、Token 优先级、CLI/内嵌混用、超时不重试与 Broker 收尾不误记管理员取消。
- 前端：25 项测试通过；TypeScript 检查和生产构建成功，仅有既有大包体积提示。
- Playwright 使用关闭调度器的临时服务和虚构配置验收：全局默认显示 30 分钟，仓库输入 45 分钟后保存为 2700 秒；详情从等待中刷新为超时待处理，展示原始截止时间和保留说明。已检查截图并关闭临时浏览器、服务。
- README、示例配置、内置组合 Prompt 和前端静态产物同步更新。没有修改真实配置、历史运行或远端 PR，没有重启真实服务。
