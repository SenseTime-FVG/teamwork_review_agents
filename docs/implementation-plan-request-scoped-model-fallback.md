# 请求级回退实施方案

1. 增加共享额度错误分类器，接入外部 Provider 与 Codex 客户端的异常元数据。
2. 在内嵌模型循环中引入运行内额度跳过名单，并在每轮请求开始重新选择主链；按候选保留 effort 兼容结果。
3. 增加请求开始、额度跳过日志及快照字段，补充中文日志标题。
4. 使用假 HTTP/SSE 传输验证错误识别，并用多轮工具调用验证候选顺序、历史连续性和运行间隔离；不调用真实模型。
5. 更新 README，执行后端回归、前端测试与构建，检查变更后按用户约定提交推送。

## 本地验收结果

- Python 全量回归：780 项通过、8 项跳过。
- UI：10 项通过，TypeScript / Vite 构建成功，预构建资源已更新。
- Python 编译检查与 `git diff --check` 通过。
- 网络均使用假传输，未请求真实模型或修改运行服务配置。
