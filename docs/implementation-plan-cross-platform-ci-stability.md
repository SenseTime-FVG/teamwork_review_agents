# 跨平台 CI 稳定性修复实施方案

## 工作流

1. 将 Windows pytest 命令中的旧测试节点 ID 替换为当前测试名称。
2. 保持现有 Windows 测试集合和工作流触发条件不变。

## 测试

1. 在 POSIX 多实例停止测试中引入标准信号常量。
2. 接受退出码 `0` 或 `-SIGTERM`，拒绝其他异常退出码。
3. 运行 Windows 工作流所引用测试的节点收集与执行验证。
4. 运行进程管理定向测试和完整测试套件。
5. 执行 Python 编译检查与 `git diff --check`。
