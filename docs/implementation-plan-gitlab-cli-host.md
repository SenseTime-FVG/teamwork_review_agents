# GitLab CLI 默认主机实施计划

1. 在统一环境解析层生成 GitLab CLI 默认主机，复用根/子 Agent 和仓库进程路径。
2. 补充默认值、显式覆盖、凭据暴露、安全 URL 解析、多仓库和临时 HOME/沙盒回归。
3. 更新平台 CLI 认证说明与文档索引，说明自动值和覆盖边界。
4. 运行定向及全量 Python 测试，并以无效测试凭据、本机拒绝转发代理验证真实 glab 路由。
5. 检查完整变更，提交并推送当前分支；不修改生产配置、发布评论或重启后台。

## 验证结果（2026-09-22）

- `TEAMWORK_TEST_GLAB_ROUTING=1 .venv/bin/pytest -o addopts='' -q`：1350 passed、8 skipped；保留两项既有依赖弃用警告。
- 新增 28 项回归，包含真实 glab 普通端口/自定义端口路由对照；测试代理只拒绝连接，不向外部转发，也不使用真实 Token。
- 临时 HOME、模型/CLI 两种运行器及 Windows 环境分离桥已验证参数传递；本轮未执行真实 Windows 沙盒验收。
- 当前部署配置只读解析确认自动主机为内网 GitLab；Token 仍进入进程、不进入 Prompt。
- 未修改生产配置或 CLI 登录态，未触发真实 Agent、远端评论或服务重启。
