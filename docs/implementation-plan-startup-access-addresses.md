# 启动访问地址提示实施方案

1. 新增跨平台网卡候选发现和访问提示格式化模块，复用现有 psutil。
2. 接入后台启动、重复启动、重启以及前台就绪提示；保持健康检查及认证行为。
3. 增加单元与隔离进程回归，更新 README 和运维说明。
4. 检查完整改动，仅提交本次文件并推送当前分支，不操作无关未跟踪文件和真实服务。

## 验收记录

- 后端全量回归：`python -m pytest -o addopts='' -q`，1084 通过、8 跳过；仅有既有 Starlette/AnyIO 弃用警告。
- 虚构网卡覆盖 IPv4/IPv6、多网卡、重复地址、停用和未知状态网卡、无候选、系统接口异常以及明确绑定的主机名/IP。
- 真实隔离 CLI 依次执行 `start`、`restart`，确认端口覆盖、不同 PID、地址输出及后台日志；未提供 Token 的本机管理请求仍返回 401，带测试 Token 返回 200，输出不包含 Token 值。
- 在当前 macOS 主机只读验证网卡发现，成功生成当前 `en0` 网卡的局域网地址和端口。未实际执行 Windows/WSL 网络可达性测试；提示不承诺这些环境的候选地址一定可达。
- 未修改真实配置、监听或认证策略，未重启真实服务；无前端变更，无需重新构建 UI。
