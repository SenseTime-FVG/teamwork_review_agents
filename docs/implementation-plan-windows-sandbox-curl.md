# Windows 沙盒 curl 兼容实施方案

1. 新增运行级 curl 发现、真实沙盒验证及环境选择模块；配置支持显式 curl 路径。
2. 在执行器创建/清理独立上下文，两种 Runner 在模型执行前探测，权限包装器只读授权候选及已选程序依赖。
3. 内嵌 PowerShell 命令处理 curl 别名，完整 CLI 注入简短运行提示，失败只反馈不重放。
4. 收紧 Git 错误分类与两条命令执行路径的调用来源判断。
5. 补充单元、Runner 集成和显式 Windows 验收测试，更新 README 与示例配置，运行 Python/UI 回归。
