# 上下文压缩实施方案

1. 添加默认开启的预算配置、模型窗口覆盖与纯历史管理模块。
2. 客户端保留上下文超限标识，运行器接入无工具摘要请求及有界恢复。
3. 固定保留系统指令、工具定义和原始任务；完整回合压缩，摘要成功后才替换历史。
4. 增加中文诊断日志、README 与配置示例，更新预构建前端资源。
5. 覆盖不可变边界、失败原子性、跨 Provider、额度回退和不重放工具；执行全量测试和构建后提交推送。

## 摘要硬上限修复

1. 将 2048 字节改为软目标，配置使用 `summary_target_bytes` 并兼容旧键。
2. 最终按完整请求预算及压缩效果验收；超限或强制恢复未缩小时，最多额外收短两次，并计入压缩总请求上限。
3. 大草稿不裁切，重新归纳其完整输入；已有摘要参与分片，任何失败保留原活跃历史。
4. 分离空摘要、预算超限、未缩小和次数耗尽的错误码，增加字节、预算及次数诊断和中文 UI 展示。
5. 补上 800 汉字摘要、整体超限恢复、中间草稿超限、取消、旧配置及错误可读性用例，执行全量验证并推送。

## 完整上下文交接与 95% 触发阈值

1. 摘要参考资料加入原始任务、系统约束、工具/格式定义、程序记录的运行身份及最近完整回合；末尾追加无工具交接请求，不变更正常任务历史结构。
2. 优先完整快照，必要时分片，每片保留固定参考资料；所有请求仍计入预算和调用上限，失败只丢弃草稿，不重放工具。
3. 完善执行状态交接与恢复提示，区分已完成、失败和待办，降低压缩后的角色漂移和重复检查。
4. 默认自动阈值改为完整窗口的 95%，保留输出预留保护、模型级窗口覆盖、缓存来源和未知模型默认值；诊断记录实际阈值与完整/分片模式。
5. 覆盖完整参考资料、分片连续性、多次文档任务压缩、不可变边界、95% 精确边界、安全预算、取消/失败和模型回退；更新配置示例及 README，运行定向与全量测试后提交推送。不修改用户部署配置，不重启现有服务；本次不改 64 轮限制或补丁工具。

### 验证结果

- Python 全量 1008 项通过、8 项跳过；最终压缩/交接/模型回退与运行器定向回归 161 项通过。
- 前端 39 项测试通过；Python 编译检查通过。前端源码与构建产物未改动。
- 模拟模型验证完整参考资料、最近回合和旧摘要的覆盖，多次压缩始终携带原始文档任务、程序身份及修改证据；未调用真实模型或重新执行现场任务，不把模拟结果表述为语义正确性的绝对保证。
- 覆盖 95% 阈值前、恰好达到、超过阈值，以及输出预留触发更低安全上限；所有摘要请求均经过自身预算检查。
